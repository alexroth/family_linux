<h1>New alternate name</h1>

<% form_for(@family_alias) do |f| %>
  <%= f.error_messages %>
    <%= render( :partial => "shared/family_alias_new", :object => f ) %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'List all alternate names', family_aliases_path %>
